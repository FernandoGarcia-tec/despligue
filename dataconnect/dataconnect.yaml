specVersion: "v1beta"
serviceId: "Especialidad"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "Especialidad-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
